version: '3.1'

services:

  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: logap-test
    #   PGDATA: /data/postgres
    # volumes:
    #    - postgres:/data/postgres
    ports:
      - 5432:5432
